<div class="hero">
    <div class="hero__main">
        <img 
            src="/profile.webp" 
            alt="Mario" 
            class="hero__avatar" 
            width="88" 
            height="88"
        />

        <div class="hero__details">
            <div class="hero__title-row">
                <h1 class="hero__name">Mario</h1>
                <span class="hero__handle">@mariollesta</span>
            </div>

            <p class="hero__description">
                Teleco Engineer · MSc in Data Science student 🇪🇸
            </p>

            <div class="hero__social">
                <a href="https://github.com/mariollesta" class="hero__social-link" target="_blank" rel="noopener noreferrer">github</a>
                <span class="hero__social-sep" aria-hidden="true">·</span>
                <a href="https://www.linkedin.com/in/mariollesta/" class="hero__social-link" target="_blank" rel="noopener noreferrer">linkedin</a>
                <span class="hero__social-sep" aria-hidden="true">·</span>
                <a href="https://x.com/mariollesta" class="hero__social-link" target="_blank" rel="noopener noreferrer">X</a>
            </div>
        </div>
    </div>

    <a href="mailto:tu-email@dominio.com" class="hero__contact-btn">
        <svg class="hero__contact-icon" xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
            <rect width="20" height="16" x="2" y="4" rx="2"/>
            <path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/>
        </svg>
        <span>Contact</span>
    </a>
</div>

<style>
    .hero {
        position: relative;
        max-width: var(--layout-reading-width);
        margin-inline: auto;
        padding: var(--spacing-2xl) 0 var(--spacing-xl);
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: var(--spacing-lg);
    }

    .hero__main {
        display: flex;
        align-items: center;
        gap: var(--spacing-lg);
    }

    .hero__avatar {
        width: 88px;
        height: 88px;
        border-radius: var(--radius-pill);
        object-fit: cover;
        background-color: var(--color-hairline);
        flex-shrink: 0;
    }

    .hero__details {
        display: flex;
        flex-direction: column;
        gap: 6px;
    }

    .hero__title-row {
        display: flex;
        align-items: baseline;
        gap: var(--spacing-sm);
        flex-wrap: wrap;
    }

    .hero__name {
        font-family: var(--font-primary);
        font-weight: 700;
        color: var(--color-ink);
        font-size: var(--text-heading-md);
        line-height: var(--text-heading-md-lh);
        letter-spacing: var(--text-heading-md-ls);
        margin: 0;
    }

    .hero__handle {
        font-family: var(--font-mono);
        font-size: var(--text-body-md);
        color: var(--color-mute);
        font-weight: 400;
    }

    .hero__description {
        font-family: var(--font-primary);
        font-size: var(--text-body-md);
        line-height: var(--text-body-md-lh);
        color: var(--color-body);
        margin: 0 0 4px 0;
    }

    .hero__social {
        display: flex;
        align-items: center;
        gap: 10px;
        font-family: var(--font-mono);
        font-size: var(--text-body-sm);
        line-height: 1;
    }

    .hero__social-link {
        color: var(--color-mute);
        text-decoration: none;
        transition: color 0.15s ease;
    }

    .hero__social-link:hover {
        color: var(--color-primary);
    }

    .hero__social-sep {
        color: var(--color-hairline);
        user-select: none;
    }

    .hero__contact-btn {
        display: inline-flex;
        align-items: center;
        gap: var(--spacing-xs);
        background-color: var(--color-primary);
        color: var(--color-on-primary);
        padding: 12px 24px;
        border-radius: var(--radius-pill);
        font-family: var(--font-primary);
        font-size: var(--text-body-sm);
        font-weight: 500;
        text-decoration: none;
        white-space: nowrap;
        flex-shrink: 0;
        transition: opacity 0.15s ease, transform 0.1s ease;
    }

    .hero__contact-btn:hover {
        opacity: 0.88;
    }

    .hero__contact-btn:active {
        transform: scale(0.97);
    }

    .hero__contact-icon {
        flex-shrink: 0;
    }

    @media (max-width: 640px) {
        .hero {
            flex-direction: column;
            align-items: flex-start;
            gap: var(--spacing-lg);
            padding: var(--spacing-xl) 0;
        }

        .hero__main {
            gap: var(--spacing-md);
        }

        .hero__avatar {
            width: 72px;
            height: 72px;
        }

        .hero__contact-btn {
            width: 100%;
            justify-content: center;
        }
    }

    @media (prefers-reduced-motion: no-preference) {
        .hero {
            animation: hero-fade-in 0.4s ease-out both;
        }

        @keyframes hero-fade-in {
            from {
                opacity: 0;
                transform: translateY(6px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    }
</style>